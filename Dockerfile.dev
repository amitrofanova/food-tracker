FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Bind to 0.0.0.0 so the dev server is reachable outside the container
CMD ["npx", "vite", "--host", "0.0.0.0"]
